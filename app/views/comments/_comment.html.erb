<% @comments.reverse.each do |comment|%>
  <div class="comment_user pl-2">
    <%= link_to comment.user.nickname, user_path(comment.user), class:"comment-nickname" %>
    ・<%= comment.created_at.strftime("%Y/%m/%d") %>
  </div>
  <div class="comment mb-3 pl-2">
    <p><%= comment.comment %><p>
  </div>
<%end%>